name: flutter_digital_id_example
description: Example app demonstrating flutter_digital_id usage for KYC / signup flows, including test vector mode for UI and backend handoff development without native entitlements.
publish_to: none

resolution: workspace

environment:
  sdk: ">=3.10.0 <4.0.0"
  flutter: ">=3.38.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_digital_id:
    path: ../

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
